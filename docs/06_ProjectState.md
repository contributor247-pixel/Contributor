# 06 — Project State (Live Tracker — Agent Updates This File)

> **What this file is for:** this is the single source of truth for *where the build actually is right now* — which steps are done, what got decided along the way, and what to do next. It exists so the project can be picked up by a different Claude Code session, a different model, or a different day with zero lost context — no re-reading the whole chat history, no re-deriving decisions, no accidentally re-doing or skipping a step.
>
> **Rule for the coding agent:** at the end of every step or sub-step in `04_MasterBuildGuide.md` — right before you report that step complete to the user — you MUST update this file: flip the step's status, fill in its "What was actually done / decided" line, note any deviation from the guide (and why), and update the "Current Position" block at the top. Do this even if the step's Testing checklist hasn't been confirmed by the user yet — mark it `🔄 Built, awaiting user test` rather than `✅ Done` until the user confirms it passed. Never skip this update, and never let this file fall behind actual git history — if you're unsure whether a step is really done, check the codebase, don't guess from memory.
>
> **What `✅ Done` means now:** per `04_MasterBuildGuide.md`'s "How to use this document" 4-phase loop (Build → Test → Bug-Find & Gap-Find → Fix & Re-test), a step is only `✅ Done` after phase 4 completes clean — passing the Testing checklist alone (phase 2) is not enough to mark it Done. If a step has passed its Testing checklist but the Bug-Find & Gap-Find pass hasn't happened yet, keep it at `🔄 Built, awaiting user test` and note in that row that phase 3/4 are still outstanding.
>
> **Rule for a brand-new agent session (different model, new chat, resumed later):** before doing anything else, read this file in full. Resume exactly from the step marked `▶️ Next up`. Do not restart, redo, or "improve" a step already marked `✅ Done` unless the user explicitly asks you to revisit it — treat completed steps as trustworthy, verified ground truth. If a step is marked `🔄 Built, awaiting user test`, ask the user whether it passed testing before moving on, rather than assuming.

---

## Current Position

- **Status:** Step 13 (Notifications & Emails for All Flows) is now fully built and verified end-to-end with Playwright across three real flows. Step 12 (Medium-style Tiptap editor) and Step 11 (Admin Dashboard, live-tested + repolished) were completed immediately before this in the same session. Steps 1-10 were also re-audited and polish-passed earlier in this same effort. `DATABASE_URL`, `AUTH_SECRET`, and real Stripe TEST-mode keys (`STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`) all set in `.env.local`. Resend key still a placeholder (email `render()` works fine, only the actual send 401s — a standing, accepted waiver since Step 2.3).
- **Next up:** Step 14 (Full Responsive Pass). Note: AuthModal (Step 2.2) was already restyled to the real token system during the Steps 1-10 audit pass — the old TODO about that is resolved. **Bugs found and fixed during Step 11's build/repolish, all pre-existing gaps unrelated to Step 11's own code:** (0) `DashboardSidebar` picked its nav set from `useSession()`, which briefly reports no role (defaults to "reader") while the session is hydrating client-side — every fresh admin/author dashboard page load would flash the wrong sidebar for a beat. Fixed by deriving the role from the URL path first (`/dashboard/admin/**` etc. already disambiguate), falling back to session role only for the bare `/dashboard` path. (1) `src/lib/queries/articles.ts` never checked article authors' account status, so suspending an author via the new admin panel would NOT have hidden their articles from any public listing/search/detail query — fixed by adding a `suspendedAuthorArticleIds()` helper applied to every public query (`getRecentArticles`, `getArticlesByCategorySlug`, `getPaginatedArticles`, `searchArticles`, `getArticleBySlug`). (2) The app had zero `not-found.tsx` anywhere, so every one of the 7 existing `notFound()` call sites (article/publication/category detail pages, author edit pages) rendered a completely blank white page instead of a real 404 — fixed by adding `src/app/not-found.tsx`, a themed 404 page with Navbar/Footer via the root layout. **Important for local dev going forward:** Stripe webhooks require `stripe listen --forward-to localhost:<port>/api/webhooks/stripe --api-key <STRIPE_SECRET_KEY>` running in a terminal, and `.env.local`'s `STRIPE_WEBHOOK_SECRET` must match that command's printed signing secret (it rotates every time `stripe listen` restarts) — the dev server must be restarted after changing it. **The Stripe CLI listener is NOT persistent across terminal/session boundaries** — it was found dead partway through Step 11's fee-split test (a purchase silently produced no ledger row because the webhook never arrived) and had to be manually restarted; always verify `ps aux | grep stripe` shows a live listener before trusting a "purchase didn't create a ledger row" result as a real bug. The Stripe CLI on this machine is authenticated to a different Stripe account by default, so always pass `--api-key` explicitly rather than relying on the CLI's stored login. **Known machine-local issue:** an unrelated project's dev server on this same machine sometimes already occupies port 3000, which is what `AUTH_URL` in `.env.local` points to — when that happens Next.js silently falls back to another port (3002, etc.) and auth/session cookies stop matching `AUTH_URL`, breaking login. User has chosen to manage this themselves (close the other project's server, or temporarily point both `AUTH_URL` and Stripe's `--forward-to` at whatever port Next actually bound, e.g. 3010, as was done for Steps 8-11's work) rather than permanently changing Contributor's port — flag this if login/OTP mysteriously stops working in a future session and the dev server didn't start on port 3000. **For testing subscription renewal/pooled-distribution logic specifically:** `stripe trigger invoice.payment_succeeded` cannot be pointed at a real locally-created subscription (it synthesizes its own unrelated objects) — call `distributePooledSubscriptionRevenue` directly with the real subscription row instead, as done for Step 10's verification. **Also confirmed again in Step 11:** this Neon instance's round-trip latency is slow enough that Playwright `waitForTimeout` calls after any server action (2000-3000ms) can catch the UI mid-"Saving.../Working..." state in a screenshot — always re-check the DB directly (or re-screenshot after a longer wait) before concluding a save/action silently failed.
- **Last updated by:** Claude Code session, 2026-09-10 (Step 13 built and verified end-to-end — email templates, webhook/admin/invite wiring, in-app notification bell — after Steps 11 and 12 were completed in the same session).
- **Tech stack lock-in (must match `00_ScopeDocument.md` §12 — do not deviate without updating that doc too):** Next.js App Router + TypeScript, Tailwind CSS, shadcn/ui, Framer Motion + GSAP/ScrollTrigger + animate.css, Neon Postgres + Drizzle ORM, Auth.js (NextAuth v5), Stripe, Resend, Vercel.
- **Component reuse/coding standards:** governed by `07_ComponentArchitectureAndStandards.md` — re-read it before building or extending any shared component (Navbar, Footer, ArticleCard, AuthModal, SearchOverlay, dashboard shell, application-state components, etc.).

---

## Key Decisions Log

> Append one row every time a step asks the agent to choose between documented options (e.g. "your choice, document which you used"), or confirms something with the user (e.g. the refined color palette, the Medium editor understanding). This is what lets a new agent avoid re-asking questions that are already answered.

| Step | Decision point | Resolution | Confirmed by user? |
|---|---|---|---|
| 2.1 | `@auth/drizzle-adapter`'s TypeScript types require a `users.image` column; our schema uses `avatarUrl` per spec | Added a nullable `image` column reserved for the adapter/future OAuth avatar sync; app UI continues to read only `avatarUrl` | Yes — user asked agent to pick the option best for the project after being given 3 choices (cast, add column, drop adapter) |
| 2.2 | Step 2.2 asks for theme-doc styling, but Step 3 (real color-theory-refined palette + tailwind tokens) hasn't run yet | Build AuthModal now using Theme Guideline Section 2's placeholder hex values directly; restyle to the shared token system once Step 3 lands | Yes — user picked "build now with placeholder tokens" over "do Step 3 first" when asked |
| 2.2 | Dialog primitive library: guide doesn't specify Radix vs Base UI vs other | This scaffold's `shadcn@latest init` installed `@base-ui/react`, not Radix — used `@base-ui/react/dialog` directly for AuthModal's custom Framer-Motion-driven enter/exit | No explicit confirmation needed — this is simply what shadcn actually installed for this project, not a discretionary choice |
| 2.5 | "Remember Me" cannot vary Auth.js v5's session/cookie expiry per sign-in through normal config (single static `session.maxAge` drives both the cookie lifetime and the displayed `expires`) | Kept one fixed 30-day session for everyone; checkbox stays in the UI (matches design reference) but is non-functional, clearly commented in `auth.ts` | Yes — user chose "drop differentiation" over a non-persistent-cookie approach or shipping a half-working custom-JWT-expiry hack, after being shown all three options |
| 2.6 | Guide's Step 2.6 text states the OTP-verification requirement explicitly for author routes but not for admin routes — ambiguous whether that's intentional | `requireRole` now also requires `twoFactorVerified` whenever `"admin"` is an allowed role, closing what would otherwise be a pre-OTP admin access gap | Yes — user chose "require full 2FA for admin routes too" after the ambiguity was flagged with both readings explained |
| 3 | Final color palette: theme doc's starting-point crimson vs. a color-theory-refined alternative | Oxblood `#8B1E3F` accent + cooler-black `#14141A` ink + darkened `#706F76` muted text; shown via a local `/palette-preview` route rather than an Artifact link | Yes — user reviewed the before/after swatch comparison and WCAG contrast table, approved as shown, explicitly asked for a localhost URL instead of an Artifact link |
| 3 | Guide says "wire tokens into tailwind.config.ts", but this scaffold's shadcn/Tailwind setup uses v4's CSS-first `@theme` config with no JS config file | Tokens defined in `src/styles/tokens.css`, mapped into `globals.css`'s `@theme inline` block instead — functionally equivalent, no `tailwind.config.ts` created | No explicit confirmation needed — this is simply what the scaffold actually uses, not a discretionary choice |
| 3 | Placeholder pages for `/about`, `/contact`, `/content` so navbar links don't 404 ahead of their real build steps | Built minimal "coming later"/"coming in Step 5" pages using a shared `ComingSoonPlaceholder` component | Yes — user chose to add placeholders now over leaving them as 404s |
| 4 | Cover image upload approach: Vercel Blob vs. base64 data-URL | Base64 data-URL storage read via `FileReader`, capped at 2MB and validated as an image MIME type — no new external service/account needed for now | Yes — user explicitly chose base64 over adding a Vercel Blob store |
| 4 | Categories are Admin-managed only per spec, but Admin dashboard (Step 11) isn't built yet — Article publish requires a category, creating a chicken-and-egg block | Seeded 7 starter categories (Technology, Culture, Business, Science, Lifestyle, Politics, Health) directly into the DB via a one-off `drizzle/seed-categories.ts` script (idempotent, `onConflictDoNothing` on slug) rather than leaving Authors fully blocked from publishing until Step 11 | Yes — user confirmed seeding now was acceptable, documented as a deviation |
| 4 | `docs/03_FolderStructure.md`'s literal file tree shows `(dashboard)/author/**` with no separate `dashboard/` folder, which as a Next.js route group produces `/author/**` URLs — contradicting `04_MasterBuildGuide.md`'s own prose and the already-built Step 3 Navbar/Sidebar links, which both assume `/dashboard/author/**` | Added a real `dashboard/` folder segment inside the `(dashboard)` route group (`(dashboard)/dashboard/author/**`) so URLs resolve to `/dashboard/author/articles` etc., matching the guide's prose and existing links, rather than rewriting the Navbar/Sidebar to match the literal (and internally inconsistent) file tree | Yes — user chose "add a real dashboard folder" over "follow the file tree literally and rewrite links," after the contradiction was flagged with both readings explained |
| 5 | Step 5's guide text requires an "optional free-text detail field" on the Report dialog, but the `reports` table (built in Step 1) has no column to store it — a real schema gap, not a discretionary choice | Added a nullable `detail` text column to `reports` via a new migration (`0002_youthful_storm.sql`), rather than silently dropping the field from the UI to match the existing schema | No explicit confirmation needed — the guide's own text requires this field to exist; omitting it would be an unauthorized scope cut, not a judgment call |
| 7 | Stripe Product/Price sync approach for AuthorPro (guide's "your choice, document which") | Inline `price_data` generated fresh on every Checkout session, reading current `platformConfig` pricing — no pre-created Stripe Product/Price objects to keep in sync with admin-editable pricing | No explicit confirmation needed — a "your choice, document which" instruction in the guide, not a discretionary UX/business call requiring the user's input |
| 7 | Local Stripe CLI was already authenticated to an unrelated Stripe account ("AromaFlare") | Used `stripe listen --api-key <real sk_test_ key>` to forward webhooks against the correct account for this project, rather than running `stripe login` (which would change the CLI's global config and affect other projects on the same machine) | Yes — user chose the `--api-key` approach over switching the CLI's stored login or skipping live webhook testing entirely |

---

## Step-by-Step Status

> Status values: `⬜ Not started` / `🔄 Built, awaiting user test` / `✅ Done` / `⚠️ Done with known issue` (explain in notes) / `⏭️ Skipped` (explain why — should be rare, this build is sequence-dependent).

### Step 0 — Accounts & Environment Setup

| Sub-step | Status | What was actually done / decided | Notes / deviations |
|---|---|---|---|
| 0.1 GitHub account + repo | ⬜ Not started | | |
| 0.2 Node.js, Git, code editor | ⬜ Not started | | |
| 0.3 Vercel account | ⬜ Not started | | |
| 0.4 Neon account + project | ⬜ Not started | | |
| 0.5 Stripe account + keys + Connect | ⬜ Not started | | |
| 0.5 (continued) Project scaffold | ✅ Done | Ran `create-next-app` (TS, Tailwind, App Router, src dir, `@/*` alias, ESLint), `shadcn@latest init`, and installed all listed deps (framer-motion, gsap, drizzle-orm, @neondatabase/serverless, drizzle-kit (dev), next-auth@beta, stripe, @stripe/stripe-js, resend, @react-email/components, zod, react-hook-form, @hookform/resolvers, lucide-react, animate.css, clsx, tailwind-merge, class-variance-authority). Copied `docs/` and `Refrence/` into the project root. `npm run build` succeeds with zero errors. Git initialized by create-next-app; a second commit added the shadcn/deps/docs changes. | Repo was NOT pushed to GitHub and no remote was set — user is completing Steps 0.1-0.4/0.6-0.9 (accounts, GitHub repo, env vars) themselves outside this workflow. When the user provides the GitHub repo URL, still need to: set the remote and push. `.env.local`/`.env.example` not yet created (Step 0.8, pending user). |
| 0.6 Resend account + API key | ⬜ Not started | | |
| 0.7 Auth.js secret | ⬜ Not started | | |
| 0.8 `.env.local` + Vercel env vars | ⬜ Not started | | |
| 0.9 Claude Code / agent setup | ⬜ Not started | | |

### Steps 1–17

| Step | Status | What was actually done / decided | Notes / deviations |
|---|---|---|---|
| 1 — DB Schema & Drizzle Setup | ✅ Done (4-phase loop complete) | Created `drizzle.config.ts`, `src/lib/db.ts` (neon-http driver), and all 16 schema files under `drizzle/schema/` (users, auth [accounts/sessions/verificationTokens], categories, tags, articles + articleAuthors + articleTags, publications, invites, subscriptions, purchases, readEvents, ledger, reports, comments, otpCodes, notifications, platformConfig) plus `drizzle/schema/index.ts` barrel with full relations(). Added `db:generate`/`db:migrate`/`db:studio`/`db:seed` npm scripts. Generated and ran the migration against the real Neon DB (20 tables confirmed present via direct query) and ran `drizzle/seed.ts`, confirmed platformConfig row has exactly `standalone 80/20` and `in_publication 60/20/20` per spec. Added `.env.example` (was missing from Step 0.8). **Bug-Find & Gap-Find pass (phase 3) found and fixed a real bug**: `drizzle/schema/index.ts`'s original `export * from "./x"` barrel style resolved every table to `undefined` for any consumer importing the barrel under `tsx`'s ESM loader (repro'd down to a single `export * from` of one bare `pgTable`, isolated as a `tsx`/esbuild ESM interop quirk with Drizzle's table objects — confirmed NOT a Next.js/Turbopack issue, the app itself was unaffected, but `drizzle-kit` tooling and any tsx/ts-node script importing the barrel would silently get `undefined`). Fixed by rewriting the barrel to use explicit named `export { x } from "./y"` re-exports instead of `export *`. Re-ran a full write/read/constraint/relational-query test suite (21 checks: FK inserts, joint-authorship multi-row articleAuthors, standalone 80/20 and in-publication 60/20/20 ledger sum-to-gross checks, `read_events` (user,article,period) unique-constraint rejection, ledger `articleId=null` zero-read case, soft-delete on comments, drizzle relational `with:` queries across category/articleAuthors/articleTags) — all pass against the real Neon DB after the fix. Re-ran `npx tsc --noEmit`, `npm run build` (both zero errors), `drizzle-kit generate` (confirms zero schema drift from the barrel rewrite), and `drizzle-kit studio` (starts and connects cleanly). All scratch/debug test scripts were deleted after use — no test artifacts left in the repo. | Deviation: added a `deprecated` boolean to `categories` (not explicitly listed in Step 1's spec) since Step 11 requires soft-delete/deprecate categories — added now to avoid a follow-up migration, same pattern the guide already uses for otpCodes/notifications. `users.id`/auth table FKs use `uuid` (not `text`) to match the guide's "id (uuid pk)" spec for users — `@auth/drizzle-adapter` isn't installed yet (that's Step 2.1), so adapter compatibility with uuid ids will be confirmed then. **Note for future schema files**: never use `export * from` in `drizzle/schema/index.ts` for a new table — always add it to the explicit named `import {...}`/`export {...}` lists, per the comment left in that file, or it will silently break any tsx-run script (seed scripts, ad-hoc data scripts) even though the Next.js app itself would still work. |
| 2.1 Auth.js config + Drizzle adapter | ✅ Done (4-phase loop complete) | Installed `@auth/drizzle-adapter`, `bcryptjs`, `@types/bcryptjs`. Wrote `src/lib/auth.ts`: DrizzleAdapter wired to our `users`/`accounts`/`sessions`/`verificationTokens` tables, Credentials provider (email+password, bcrypt compare against `passwordHash`), `session: {strategy: "jwt"}`, and `jwt`/`session` callbacks persisting `{id, email, role, emailVerified, status, twoFactorVerified}` into the token/session (the `trigger==="update"` branch is pre-wired for Step 2.4's OTP step to flip `twoFactorVerified` via `session.update()` without re-issuing credentials). Exported `handlers, signIn, signOut, auth`, wired into `src/app/api/auth/[...nextauth]/route.ts`. Added `src/types/next-auth.d.ts` module augmentation for the custom session/JWT fields. **Bug-find/gap-find**: TypeScript caught that `@auth/drizzle-adapter`'s `DefaultPostgresUsersTable` type requires an `image` column, which our `users` table (built to `00_ScopeDocument.md`'s spec, using `avatarUrl`) didn't have. Asked the user how to resolve it (cast vs. add unused column vs. drop the adapter) — user asked me to choose in the project's best interest; added a nullable `image` column reserved for the adapter/future OAuth (migration `0001_chunky_colonel_america.sql`), leaving `avatarUrl` as the only column the app's own UI ever reads, since this is honest to the type system, costs nothing today, and de-risks any future OAuth provider work without touching documented scope. Live-tested against the running dev server (not just type-checked): real signup-less test user created directly in DB, full CSRF-protected `/api/auth/callback/credentials` sign-in succeeded and issued a valid session cookie; `/api/auth/session` correctly returned all required fields; wrong-password and nonexistent-email attempts both correctly rejected with `error=CredentialsSignin` and no session cookie; confirmed the `sessions`/`accounts` tables stay empty under JWT+Credentials (no unwanted adapter writes). `tsc --noEmit`, `npm run build`, and `drizzle-kit migrate` (idempotent re-run, zero errors) all pass. No UI built in this sub-step, per the guide's explicit instruction ("configuration only"). | Deviation: added `users.image` column (nullable, unused by app UI) to satisfy `@auth/drizzle-adapter`'s TypeScript contract — see decision above, logged in Key Decisions Log. |
| 2.2 Signup flow + auth modal | ✅ Done (4-phase loop complete) | Built `src/components/shared/AuthModal.tsx` (login/signup split-panel per `Refrence/login.png`, using Base UI's `Dialog` primitives — this project's shadcn setup uses `@base-ui/react`, not Radix, corrected after an initial wrong-library draft caught by `tsc`), `src/hooks/use-auth-modal.tsx` (plain React Context provider/hook — no state library added since none is in the guide's dependency list), `src/lib/validators/auth.ts` (zod schemas for login/signup, password-confirmation `.refine`), `src/lib/actions/auth.ts` (signup Server Action: zod validate, duplicate-email check, bcrypt hash, insert with `emailVerified: null`, calls the email stub), `src/lib/email.ts` (`sendVerificationEmail` stub — logs only, real Resend wiring is Step 2.3), and `src/components/shared/Providers.tsx` (SessionProvider + AuthModalProvider + `<AuthModal/>`, composed into root layout). Added a `TempAuthModalTrigger` component + wired it into the still-default `page.tsx` purely so the modal is manually testable before Step 3's real Navbar exists (explicitly commented for removal then, per the guide's own instruction to just export the hook for now). Installed shadcn `dialog`/`input`/`checkbox`/`label` primitives. Styled with the theme doc's Section 2 placeholder hex tokens directly (ink/crimson/etc.) since Step 3 (which wires the real color-theory-refined palette into `tailwind.config.ts`) hasn't run yet — user explicitly chose to proceed this way rather than reorder the steps; this component will get a small restyle pass to the shared token system once Step 3 lands. **Live browser-driven QA** (Puppeteer against the real local Chrome, driving the actual running dev server — not just unit/type checks): homepage renders modal triggers; login modal opens matching the reference layout; split-panel correctly stacks on mobile (390px) with the dark panel above the form, not squeezed; dark-panel Sign Up button correctly flips to the signup form; signup client-side validation catches empty-required-fields and password-mismatch without a server round-trip; a real signup end-to-end creates a correct DB row (name/email/role/`emailVerified:null`) and shows the success banner, auto-flipping back to login after ~2.5s; duplicate-email signup is correctly rejected with the right message; a real login end-to-end (after manually marking the test row verified, since Step 2.3 doesn't exist yet) succeeds, closes the modal, and reflects the session in the UI; Escape and outside-click both close the modal; `prefers-reduced-motion` doesn't break anything; keyboard focus lands inside the modal on open (Base UI's built-in focus trap) and Tab moves within it. All test users deleted from the DB afterward, all scratch Puppeteer scripts deleted (installed in an OS temp directory, never touched the project's own `package.json`/`node_modules`). `tsc --noEmit`, `eslint`, and `npm run build` all pass with zero errors/warnings. | Deviations: (1) used Base UI's dialog primitives directly (`@base-ui/react/dialog`) instead of Radix, since that's what this scaffold's shadcn setup actually installed — caught this by type-checking a first draft that assumed Radix and got real compile errors; (2) used plain React Context for `useAuthModal` instead of a state library, since none is in the documented tech stack; (3) styled directly against Theme Guideline Section 2's placeholder hex values rather than shared Tailwind tokens, since Step 3 (palette confirmation + token wiring) hasn't run yet — user confirmed proceeding this way; a follow-up restyle to the real token system happens naturally once Step 3 lands. |
| 2.3 Email verification | ✅ Done (4-phase loop complete; real-inbox delivery explicitly waived by user) | Built `src/lib/resend.ts` (client init), `src/emails/verify-email.tsx` (React Email template: on-brand card, crimson CTA button, 24h-expiry notice, email-client-safe font fallback stacks standing in for Fraunces/Inter per the guide's "where email-client-safe" caveat), replaced the Step 2.2 stub in `src/lib/email.ts` with a real implementation (generates a token via `crypto.randomBytes(32)`, stores it in `verification_tokens` with a 24h expiry, renders the template, calls `resend.emails.send`). Built `src/app/(auth)/verify-email/page.tsx` (server component: reads `token`/`email` query params, calls the new `verifyEmailAction`) + `VerifyEmailClient.tsx` (renders success/invalid/expired states, "Continue to sign in" opens AuthModal in login mode, "Resend verification email" calls `resendVerificationAction`). Built `src/lib/actions/verify-email.ts`: `verifyEmailAction` (looks up the token, deletes it immediately on any use — valid or expired — so a leaked expired link can't be retried forever, sets `emailVerified` only if not expired) and `resendVerificationAction` (no-op success for a nonexistent email to avoid account-enumeration, rejects if already verified, otherwise resends). **Bug-find caught a real, non-obvious issue**: the Resend SDK's `emails.send()` resolves with `{data, error}` and never throws on an API-level failure (bad key, suspended domain, etc.) — the original code ignored `error` entirely, so a Resend outage would have silently told users "check your email" for a message that was never sent, with signup reporting success and no way to diagnose it. Fixed by checking `error` and throwing in `sendVerificationEmail`, then having both `signupAction` (still creates the account — never rolls back a real user record over an email hiccup — but returns `emailSendFailed: true`) and `resendVerificationAction` catch it and return a typed failure rather than letting it cross the Server Action boundary uncaught (per `07_ComponentArchitectureAndStandards.md`'s error-handling convention). `AuthModal`'s signup success state now shows an amber warning variant when `emailSendFailed` is true instead of claiming an email was sent. **Testing performed** (against the real Neon DB and a real, correctly-rejected Resend API call — the placeholder key returns a genuine 401 from Resend's servers, so the error-handling path itself is fully exercised): React Email template renders correctly (link, button text, name interpolation, empty-name fallback) via `@react-email/render`; full token lifecycle via real Server Action calls — valid token verifies and consumes itself, reused token rejected, expired token rejected AND consumed, bogus token rejected, resend-on-already-verified rejected with a clear message, resend-on-nonexistent-email doesn't leak account existence; `signupAction`'s new `emailSendFailed` path confirmed via a live signup with the placeholder key (account still created, token still written, flag correctly set); all three `/verify-email` page states (success/expired/invalid, plus no-token) live-tested via curl against the real dev server, confirming exact HTML content and, for the success case, the actual DB row update; resend-button UI live-tested via headless-Chrome (shows "Sending..." while in flight, then the correct error message on failure, matching the theme's error-state color). All test users/tokens deleted afterward, DB confirmed empty. `tsc`, `eslint`, and `npm run build` all pass with zero errors. | User explicitly instructed to skip real-email-inbox delivery testing and mark this step done regardless (2026-09-05) — every other aspect of the guide's Step 2.3 spec is built and verified. If real-send behavior ever needs checking later, that's the one remaining manual check once real Resend credentials exist — not a rebuild. |
| 2.4 OTP-based 2FA | ✅ Done (4-phase loop complete; real-inbox delivery waived per user's Step 2.3 instruction, same reasoning applies here) | Built `src/lib/otp.ts` (`generateOtp`: random 6-digit code, SHA-256 hashed, 10-min expiry, sends via the same Resend pattern as Step 2.3, including the same error-checking fix; `verifyOtp`: matches by userId+codeHash among unconsumed rows, checks expiry, marks consumed). Built `src/emails/otp-code.tsx` (themed template, large crimson code display). Updated `src/lib/auth.ts`: `authorize()` now calls `generateOtp` for author/admin roles right after password validation (wrapped in try/catch so a Resend failure never blocks login — the /verify-otp page's own resend button is the recovery path), and the `jwt` callback sets `twoFactorVerified: user.role === "reader"` immediately (Readers always pass; Author/Admin start false and must complete OTP). Built `src/app/(auth)/verify-otp/page.tsx` (server guard: redirects home if unauthenticated or already 2FA-verified) + `VerifyOtpClient.tsx` (6-digit input, calls `submitOtpAction`, on success calls `useSession().update({twoFactorVerified:true})` then routes home) and `src/lib/actions/otp.ts` (`submitOtpAction`, `resendOtpAction` with a 60s cooldown keyed off the most recent `otp_codes` row's `createdAt`, regardless of whether that row's email actually sent). Updated `AuthModal`'s `LoginForm` to check the post-signIn session's `twoFactorVerified` and redirect to `/verify-otp` when false. **Bug-find caught a real, intermittent bug**: the redirect was originally issued *after* calling `onSuccess()` (which closes the modal), but closing flips `useAuthModal`'s `isOpen` to false, which unmounts the `LoginForm` subtree immediately since it isn't a direct `AnimatePresence`-animated child — `router.push` calls made after that unmount were silently dropped roughly half the time. Fixed by reordering (navigate before closing) and confirmed reliable across many independent fresh-account attempts after the fix. **Testing performed**: full DB-level OTP logic suite (7 checks — valid/reused/wrong/expired/cross-user-code correctness) against the real Neon DB, run twice with identical results; live end-to-end browser flow (separate isolated browser contexts per test identity, polling-based waits rather than fixed timeouts to avoid dev-mode compile-time flakiness) confirmed: Reader login skips OTP entirely and lands signed-in immediately; Author login redirects to `/verify-otp`; wrong code shows the correct inline error and does not advance; the correct code (manually seeded, since Resend's placeholder key can't actually deliver one) elevates the session and redirects home showing signed-in state; unauthenticated and already-2FA-verified visitors are both redirected away from `/verify-otp`; mobile (390px) OTP page layout renders correctly. All test users/otp_codes deleted afterward, confirmed empty. `tsc --noEmit` and `npm run build` both pass with zero errors (`/verify-otp` registered as a dynamic route alongside `/verify-email` and the NextAuth catch-all). | During this step's testing, ~47 orphaned headless Chrome processes accumulated from test scripts that didn't reach `browser.close()` on early exceptions, which starved the dev server of memory/CPU and caused a misleading string of apparent test failures (the app was never actually broken — direct API checks via curl confirmed correct session state throughout; only the Puppeteer-driven browser assertions were unreliable while the machine was resource-starved). Cleaned up and re-verified cleanly once resources recovered. **Process note for future testing in this project**: always wrap Puppeteer test scripts in `try/finally { await browser.close() }`, and prefer polling-based waits (`waitForUrl`/`waitForText` helpers) over fixed `setTimeout` delays when asserting on redirects or async UI state — dev-mode's on-demand route compilation can add several seconds of latency unpredictably, which fixed short waits will intermittently miss even when the underlying app logic is correct. |
| 2.5 Login flow wiring | ✅ Done (4-phase loop complete) | The `signIn("credentials", ...)` wiring, OTP routing, and wrong-password error already existed from Steps 2.1/2.2/2.4 — this step added the three still-missing pieces from the guide's spec. `src/lib/auth.ts`: `authorize()` now throws distinct `CredentialsSignin` subclasses (`InvalidCredentialsError`/`EmailNotVerifiedError`/`AccountSuspendedError`, each with its own `code`) instead of returning `null` for every failure, checked in order suspended → unverified → wrong-password (suspended is checked first since it's the more final reason to refuse). `AuthModal`'s `LoginForm` now branches on `result.code` from `signIn(..., {redirect:false})` to show the exact required message per case, including a working "Resend verification email" link (wired to the existing `resendVerificationAction` from Step 2.3) for the unverified case. **Investigated and consciously did NOT implement** "Remember Me" extending session length: Auth.js v5's `session.maxAge`/cookie expiry is a single static value used for every session's cookie lifetime AND its displayed `expires` field (confirmed by reading `@auth/core/lib/actions/session.js` — both come from the same static config, not from anything per-request). A custom `jwt.encode()` with a shorter per-login internal token expiry was built and tested, but rejected before committing: it would have made the JWT silently stop decrypting early while the cookie and the session response both kept claiming the full 30 days — a confusing, half-working result. Asked the user how to proceed with three options (drop it / use a non-persistent session-only cookie when unchecked / ship the misleading hack); user chose to drop the differentiation and keep one fixed 30-day session length for everyone (`SESSION_MAX_AGE` in `auth.ts`), with the "Remember Me" checkbox staying in the UI only to match the design reference, clearly commented as non-functional. **Testing performed**: all three new error codes (`account-suspended`, `email-not-verified`, `invalid-credentials`) verified via direct curl calls to `/api/auth/callback/credentials` against real suspended/unverified/wrong-password test accounts; live browser test confirmed the exact required UI copy for each case (including the literal "Your account has been suspended, contact support." string), the resend-verification link appears only for the unverified case and successfully triggers `resendVerificationAction`, and session expiry is now consistently 30 days regardless of the checkbox (no more of the misleading variance). All test users deleted afterward, DB confirmed empty. `tsc --noEmit` and `npm run build` both pass with zero errors. | "Remember Me" does not currently change session length — see the decision above and the comment on `SESSION_MAX_AGE` in `src/lib/auth.ts`. If real differentiated session lengths are ever required, the non-persistent-cookie-when-unchecked approach (option 2 above) is the most promising path, but wasn't built since the user chose to drop it for now. |
| 2.6 Role-based route protection | ✅ Done (4-phase loop complete) | Built `src/lib/permissions.ts`: `requireAuth()` (throws `UnauthenticatedError` if no session), `requireRole(role)` (throws `ForbiddenError` if the session's role isn't in the allowed set), `requireAuthorPro()` (queries `subscriptions` fresh every call for an ACTIVE row with `type=author_pro` AND `currentPeriodEnd` still in the future — never trusts a cached role/flag, so a lapsed-but-not-yet-cancelled subscription correctly fails), `requireVerifiedAuthor()` (role author/admin + `emailVerified` + `twoFactorVerified`, the full Flow C bar). No real `(dashboard)` routes exist yet (Step 3+ builds those) so this step delivers the reusable guard library itself, ready to apply. Built `src/lib/require-page-auth.ts` — thin page-level wrappers that catch the permissions.ts errors and `redirect()`: unauthenticated → `/?authRequired=1`, forbidden (authenticated but unauthorized) → `/` plainly. Built `src/components/shared/AuthRequiredListener.tsx` (mounted in `Providers.tsx` inside a `Suspense` boundary, required for `useSearchParams()`) which auto-opens the AuthModal in login mode when it sees `?authRequired=1`, then strips the param via `router.replace` — satisfies the guide's point 4 (never a bare 404/blank page for an unauthenticated visitor). **Bug-find found and fixed a real gap**: the guide's literal text states the OTP-verification requirement explicitly for author routes ("role=author or admin with email+OTP verified") but only says "require role=admin" for admin routes, without repeating the OTP clause — since `auth.ts` already sends admin through the identical OTP-per-login flow as Author, a pre-OTP admin session was passing `requireRole("admin")` and reaching admin-gated content. Flagged to the user as an ambiguity in the guide rather than assumed either way; user confirmed admin routes should also require full 2FA given how sensitive admin actions are. Fixed by having `requireRole` additionally check `twoFactorVerified` whenever `"admin"` is one of the allowed roles. **Testing performed**: a 7-case DB-level test suite for `requireAuthorPro`'s exact subscription query (no subscription, active+unexpired, active-but-expired/lapsed, cancelled-but-unexpired, wrong-type, another-user's-subscription-doesn't-leak, the-other-user's-own-subscription-does-work-for-them) — all pass against the real Neon DB. Live browser/API testing via temporary `/test-permissions/*` routes (removed after verification, per user's choice) and a temporary Server Action, covering: unauthenticated visit → 307 redirect to `/?authRequired=1` → AuthModal auto-opens → URL cleaned up afterward (screenshot-verified); Reader passes `requireAuth` but is correctly forbidden from admin- and author-gated routes; pre-OTP Author is correctly forbidden from `requireVerifiedAuthor` and `requireAuthorPro` routes; the full real login→OTP→session-elevation loop then correctly grants access; pre-OTP Admin is correctly forbidden from `requireRole("admin")`-gated content (confirms the bug-find fix) and a fully-OTP-verified Admin correctly passes; a Server Action called directly (not through any UI button wiring assumption, exercising the real RPC path) is rejected server-side for both a Reader calling an admin-only action and an unauthenticated visitor calling one, with the exact typed error codes (`forbidden`/`unauthenticated`) rather than a thrown exception leaking across the boundary. All test users/subscriptions/otp_codes deleted afterward, DB confirmed empty, temporary test routes/action fully removed. `tsc --noEmit` and `npm run build` both pass with zero errors, confirmed clean after test-route removal (had to clear a stale `.next` type-cache reference to the deleted routes first). | Deviation/finding logged above: `requireRole` now also enforces `twoFactorVerified` for admin, beyond the guide's literal wording — user-confirmed. No real dashboard routes exist yet to wire these guards onto for real; that happens naturally as Step 3+ builds `(dashboard)/author/**`, `(dashboard)/reader/**`, `(dashboard)/admin/**` — each of those pages must call the appropriate `require*ForPage()` at the top, and their Server Actions must call the matching `require*()` from `permissions.ts` directly. |
| 3 — Core Layout Shell (Navbar/Footer/Tokens) | ✅ Done (4-phase loop complete) | **Palette confirmed** (see Decisions Log) — refined from the theme doc's starting-point crimson `#C8102E` to an oxblood `#8B1E3F` accent, plus a cooler-black ink `#14141A` and a darkened muted-text `#706F76` for WCAG AA margin; shown to the user as a local `/palette-preview` route (not an Artifact link, per user's explicit preference) with before/after swatches and a WCAG contrast table, approved as-is. Wired the full token set into `src/styles/tokens.css` (new file) and Tailwind v4's `@theme inline` block in `globals.css` — note the guide's literal instruction to use `tailwind.config.ts` doesn't apply here since this scaffold uses Tailwind v4's CSS-first config (no JS config file), so tokens are exposed as CSS custom properties consumed via `@theme inline` instead; shadcn's own `--primary`/`--border`/etc. tokens are now redirected to resolve from the Contributor palette (not run as a parallel system) so shadcn primitives automatically match. `--radius` set to 4px per Section 5's "sharp, editorial" requirement. Loaded Fraunces + Inter via `next/font/google` in the root layout, replacing the scaffold's default Geist fonts. Built `Navbar.tsx` (sticky, scroll-shadow past 40px, mobile hamburger drawer with Framer Motion slide-in + scrim, search/cart/account icon cluster, session-aware login vs. account-dropdown state) and `Footer.tsx` (newsletter capture stub with real validation, 4-column layout collapsing to 1 on mobile, social icons) matching `Refrence/home_ref.png`. Built `(marketing)/layout.tsx` wiring both, and `(dashboard)/layout.tsx` + `DashboardShell.tsx` + `DashboardSidebar.tsx`/`DashboardTopbar.tsx` (role-aware nav sets, mobile slide-in sidebar) as basic functional shells per the guide's explicit "full polish is Step 15" scope, gated by Step 2.6's `requireAuthForPage()` now put to real use for the first time. Added placeholder pages for `/about`, `/contact`, `/content` (user-approved) so the navbar's links never 404 ahead of their real build steps, using a new shared `ComingSoonPlaceholder` component. Removed `TempAuthModalTrigger` (Step 2.2's stand-in, explicitly marked for removal once a real Navbar existed) and the default `create-next-app` homepage content. **Bug-find found and fixed two real defects**: (1) `TempAuthModalTrigger`'s fixed-position buttons visually overlapped the new Navbar's icon cluster — removed; (2) `--color-overlay-scrim` (and several other tokens: `--color-bg`, the `-dark` variants) were defined in `tokens.css` but never mapped into the `@theme inline` block, so `bg-overlay-scrim` silently resolved to no background at all — the mobile nav drawer's scrim was invisible until this was fixed. lucide-react v1.x turned out to have dropped all brand/social icons (a breaking change from earlier versions) — added a tiny inline-SVG `SocialIcon` component instead of pulling in another dependency for four static glyphs. **Testing performed**: full desktop/tablet/mobile responsive pass (1440/768/390px) via headless-Chrome screenshots and DOM assertions — sticky navbar shadow-on-scroll, mobile hamburger drawer open/close with correctly-dimming scrim, footer newsletter validation (empty-email rejected, valid submit confirmed) and column stacking, placeholder routes render without 404. Confirmed the full authenticated dashboard shell end-to-end: login → dashboard route access → correct role-specific sidebar nav → topbar shows account email → mobile sidebar slide-in/out — after a lengthy detour chasing what looked like session/cookie bugs that turned out to be a **test-script selector ambiguity** (the Footer's own newsletter `<form>` also has a `type="email"` input and `type="submit"` button, so any unscoped `input[type="email"]`/`button[type="submit"]` selector silently grabbed the Footer's fields instead of the login modal's — confirmed via direct curl against the server that the actual app/session logic was correct throughout; fixed by scoping test selectors to the element nearest the modal's own heading). `tsc --noEmit` and `npm run build` both pass with zero errors. | Deviation: `tailwind.config.ts` was never created since this project's Tailwind v4 scaffold uses CSS-first `@theme` config instead — tokens live in `src/styles/tokens.css` + `globals.css`'s `@theme inline` block, functionally equivalent to what the guide describes for a v3 project. **Process note for future testing in this project**: the Footer (present on every marketing page) has its own `<form>` with a `type="email"` input and `type="submit"` button — any Puppeteer test interacting with an unrelated form (e.g., the AuthModal) MUST scope selectors to that specific form/dialog (e.g., find the element nearest a unique heading text) rather than a bare `input[type="email"]` or `button[type="submit"]`, which will silently match the Footer's fields first. |
| 4 — Article CRUD (Free Author) | ✅ Done (4-phase loop complete) | Built `src/lib/slugify.ts` and `src/lib/validators/article.ts` (zod `articleSchema`: title/body/categoryId/tags/coAuthorIds/coverImageUrl/status). Built `src/lib/actions/article.ts` (`createArticleAction`, `updateArticleAction`, `deleteArticleAction`, `searchAuthorsAction`) and `src/lib/actions/categories.ts` (`getPublishableCategoriesAction`, filters `deprecated=false`). Seeded 7 starter categories via `drizzle/seed-categories.ts` (new `db:seed-categories` npm script) since Admin category management (Step 11) doesn't exist yet. Built `src/components/editor/ArticleForm.tsx` (shared create/edit form: title, body textarea with a "Plain text for now — Step 12" note, required category select, free-form Enter/comma tag input with pill removal, base64 cover-image upload via `FileReader` capped at 2MB, co-author search-and-add debounced 300ms against `searchAuthorsAction`, Publish/Save Draft buttons disabled if no categories exist) and `src/components/shared/EmptyState.tsx` (shared empty-state component per Theme Guideline Section 8.4). Built the three pages at `(dashboard)/dashboard/author/articles/{page.tsx, new/page.tsx, [id]/edit/page.tsx}` plus `DeleteArticleButton.tsx` (client component, `window.confirm` + `deleteArticleAction` + `router.refresh()`). Enforced server-side, with no UI bypass path: (1) `isPremium` always forced `false` whenever `coAuthorIds.length > 0` via a `resolveIsPremium()` helper, regardless of any co-author's AuthorPro status — hard rule per `00_ScopeDocument.md` §7.1; (2) any non-joint `isPremium:true` request is checked against `requireAuthorPro()` and silently downgraded to `false` if the caller isn't an active AuthorPro subscriber (the Premium toggle itself isn't in this step's UI yet — Step 8 adds it — so this enforcement path is currently unreachable from the built form but is in place ahead of that UI); (3) delete is a soft-delete (`status` set to `"unpublished"`, row never removed) to preserve ledger/report history integrity; (4) publish sets `status="published"`, `publishedAt=now()`, and a unique slug via a collision-suffix loop (`generateUniqueSlug`). **Critical bug found and fixed (routing)**: `docs/03_FolderStructure.md`'s literal tree has no `dashboard/` folder inside the `(dashboard)` route group, so the pages initially built at `(dashboard)/author/**` resolved to `/author/articles` instead of `/dashboard/author/articles` — confirmed via `npm run build`'s route-listing output, and contradicting both the guide's own prose and the already-built Step 3 Navbar/Sidebar links (which assume `/dashboard/...`). Flagged to the user with both fixes explained; user chose to add a real `dashboard/` folder (see Decisions Log) — moved all four files, fixed the two relative-import depths that changed (`page.tsx`, `[id]/edit/page.tsx`; `new/page.tsx` and `DeleteArticleButton.tsx` needed no changes, `@/`-aliased imports only), confirmed via a clean `.next`-cleared rebuild that routes now list correctly as `/dashboard/author/articles`, `/dashboard/author/articles/new`, `/dashboard/author/articles/[id]/edit`. **Other bugs found and fixed during self-review**: removed a dead empty `if` block in `createArticleAction`; `searchAuthorsAction` didn't exclude the calling user from its own co-author search results (an Author could find and add themselves) — added a `.filter((u) => u.id !== session.user.id)`. **Live end-to-end testing performed** (headless Chrome via Puppeteer against the real running dev server and real Neon DB, driving actual login+OTP — two temporary verified-Author test accounts created directly in the DB, OTP codes read via direct DB query rather than a real inbox since Resend delivery is still waived per Step 2.3's standing decision): full login→OTP→session flow; `/dashboard/author/articles` loads at the corrected URL and renders the empty state with correct copy/CTA when the Author has no articles; creating an article with a category and two comma/Enter-added tags, confirmed no "Premium" text/toggle anywhere in the form; publish correctly redirects to "My Articles" showing the new row with status "published"; adding a co-author via the search-and-click flow correctly persists both `article_authors` rows (DB-verified count=2) and forces `is_premium=false` (DB-verified); edit flow correctly pre-fills the existing title/body/category/tags/co-authors and a save correctly updates the row and reflects in the list; delete correctly removes the article from the visible "My Articles" list while the DB row still exists with `status='unpublished'` (DB-verified, confirming true soft-delete, not a hard delete). All test users, test articles, and orphaned test tags removed from the DB afterward via a one-off cleanup script (also deleted); all scratch Puppeteer/seed scripts deleted from the repo, confirmed via `git status` that only the real Step 4 source files remain untracked/modified. `npx tsc --noEmit` and `npm run build` both pass with zero errors. | Deviations logged in Decisions Log above: base64 cover-image storage (not Vercel Blob); 7 starter categories seeded directly since Admin category management doesn't exist yet; added a real `dashboard/` folder to `(dashboard)/dashboard/author/**` to resolve a folder-structure/URL-path contradiction between `03_FolderStructure.md` and the rest of the build. **Process note for future steps**: any new page under `(dashboard)` must live at `(dashboard)/dashboard/<role>/**`, not `(dashboard)/<role>/**` — the route group itself contributes no URL segment, so the literal `dashboard` folder is what makes `/dashboard/...` URLs work. Puppeteer testing in this project requires `executablePath` pointed at a system Chrome install (`C:\Program Files\Google\Chrome\Application\chrome.exe`) — `npx puppeteer browsers install chrome`'s own bundled-Chrome download has repeatedly failed to extract cleanly/completely on this machine (missing `chrome.exe` despite a reported success), so don't rely on it; go straight to a system browser via `executablePath`. Also confirmed again: the Footer's newsletter form collides with unscoped `input[type="email"]` selectors — the AuthModal's own inputs must be scoped under `[role="dialog"]`. |
| 5 — Homepage + Content Listing + Single Article + Comments + Report | ✅ Done (4-phase loop complete) | Built the reusable shared component set the guide's Section 1 doctrine calls for (each used across multiple pages, never forked): `ArticleCard.tsx` (cover, eyebrow category+date via `timeAgo()`, 2-line-clamp title/excerpt, byline+avatar, Premium lock badge), `Avatar.tsx` (base64-image-or-initials fallback), `CategoryPill.tsx`, `PremiumBadge.tsx`, `Pagination.tsx` (numbered pills + ellipsis collapsing + Next link), `SectionContainer.tsx`, `HeroBand.tsx` (red band used by both listing pages), `HomeHero.tsx` (dark hero + 4-thumbnail rail), `EditorsPicks.tsx` (featured story + numbered 01-04 list), `ArticleListingGrid.tsx` (shared paginated grid used by both `/content` and `/content/[category]`), and a small `useToast` provider (`src/hooks/use-toast.tsx`, wired into `Providers.tsx`) for the report-submission confirmation. Built `src/lib/queries/articles.ts` as the single data-access module for all public article reads (`getRecentArticles`, `getArticlesByCategorySlug`, `getPaginatedArticles`, `getArticleBySlug`), joining `articleAuthors`→`users` once per batch rather than N+1 per card. Built the homepage (`(marketing)/page.tsx`): most-recent-published article as hero + 4-thumbnail rail, several 3-column `ArticleCard` grids, a promo banner linking to AuthorPro, and an Editor's Picks module, all from real DB data with a shared `EmptyState` fallback when zero articles are published. Built `/content` (paginated all-articles grid, 12/page) and `/content/[category]` (same grid filtered by category slug, `notFound()` on an unknown slug, hero headline swapped to the category name). Built the single article page `/article/[slug]`: byline+avatar, cover image, body (rendered from the stored `{html}` JSON via `dangerouslySetInnerHTML`, plain-text/pre-wrap since the real rich-text editor is Step 12), a Report link, a Related Articles grid (`getRecentArticles` excluding the current article), and a comment section. Built comments per `00_ScopeDocument.md` Section 4: `src/lib/actions/comment.ts` (`postCommentAction` returns the real created row's id/createdAt rather than trusting a client-generated one; `deleteCommentAction` soft-deletes via `deletedAt`, permission-checked as own-comment-or-admin; `getCommentsForArticle`), flat/non-threaded, any logged-in user can post (`requireAuth()`, not role-gated), unauthenticated visitors get a "Log in to leave a comment" button that opens the existing AuthModal. Built the Report feature per Section 8 and Flow H: `src/app/api/reports/route.ts` (POST, `requireAuth()`-gated, rejects a second **open** report from the same user on the same article with 409, allows a new one if the prior was already actioned/dismissed) plus `ReportDialog.tsx` (reason select from the schema's exact enum, optional detail textarea, success shows a toast and closes the dialog, prompts login via AuthModal if the visitor isn't authenticated). **Real schema gap found and fixed**: the guide's Step 5 text explicitly requires an optional free-text "detail" field on the report form, but the `reports` table built in Step 1 has no such column — added a nullable `detail` text column via migration `0002_youthful_storm.sql` (see Decisions Log) rather than quietly dropping the field to match the existing schema, since the guide's requirement is explicit, not discretionary. **Also found and fixed**: `articles.excerpt` was never populated by Step 4's `createArticleAction`/`updateArticleAction` (needed by every `ArticleCard`) — added a `buildExcerpt()` helper (plain-text truncation to ~200 chars) called from both actions. **Critical bug found and fixed via live DB-level testing (not just UI clicking)**: comments posted during the same session used a client-generated `crypto.randomUUID()` as a placeholder id for optimistic UI display, but that fake id was never reconciled with the real server-generated id — so deleting a comment posted in the same session silently no-opped (`deleteCommentAction` correctly returned "not found" for the bogus id, but the failure was easy to miss behind Puppeteer-timing noise during testing). Fixed by having `postCommentAction` `.returning()` the real `{id, createdAt}` and having `CommentForm` use that instead of a client-side fake — confirmed fixed via a direct DB query showing `deleted_at` correctly set after using the real id. **Live end-to-end testing performed** (headless Chrome via Puppeteer against the real dev server and real Neon DB, 18 real published test articles seeded across all 7 categories, real login+OTP via a verified test Author): homepage renders hero+rail+grids+Editor's Picks+promo banner with zero console/page errors, confirmed via full-page screenshots at 1440px and 390px; `/content` renders correct pagination (18 articles / 12 per page = page "1 2 NEXT"), confirmed via screenshot; `/article/[slug]` renders byline/body/related articles/report link/comments, confirmed via screenshot at both viewport widths; posting a comment while logged in makes it appear immediately without a page reload and persists in the DB (author-joined query verified); submitting a report with a reason creates the correct DB row (`reason`, `status="open"`) and shows the "Thanks, we'll review this." toast, dialog closes; attempting a second report on the same article while the first is still open is correctly rejected with a 409 and the exact "You already have an open report for this article" message; deleting your own comment (via the real returned id, post-fix) correctly soft-deletes it (`deletedAt` set, row preserved) and removes it from the visible list. All test articles/users/comments/reports removed from the DB afterward via a one-off cleanup script (deleted); all scratch Puppeteer/seed scripts and screenshots deleted from the repo, confirmed via `git status` that only real Step 5 source files remain. `npx tsc --noEmit` and `npm run build` both pass with zero errors; final build's route listing confirms `/`, `/content`, `/content/[category]`, `/article/[slug]`, and `/api/reports` all registered correctly. | Deviation logged in Decisions Log: added `reports.detail` column (migration `0002_youthful_storm.sql`) to satisfy the guide's explicit optional-detail-field requirement, which the Step 1 schema had missed. **Process note for future steps**: any comment/entity created via a Server Action for optimistic client-side display must use the server's real returned id (via `.returning()`), never a client-generated placeholder — a mismatched id will silently break any later action keyed by that id (delete, edit, etc.) without throwing anywhere obvious, and is easy to mistake for test flakiness rather than a real bug. Cover images/avatars are rendered via plain `<img>` tags project-wide (not `next/image`), since covers are stored as base64 data URLs which `next/image`'s optimizer does not support. |
| 6 — Search | ✅ Done (4-phase loop complete) | Added `searchArticles(query, limit)` and `getPopularCategoryPills(limit)` to `src/lib/queries/articles.ts` (published-only, `ILIKE` match against title/excerpt, plus a tag-name `ILIKE` sub-query joined by article id, since Postgres full-text search isn't needed at Phase-1 scale). Built `src/app/api/search/route.ts` (GET, `?q=` param, delegates straight to `searchArticles`, capped at 24). Built `src/components/shared/SearchOverlay.tsx`: fixed full-viewport (`inset-0`) white overlay, giant serif input autofocused on open, popular-category pills (real top-5-by-published-article-count categories via `getPopularCategoryPills`, not hardcoded words), a live 300ms-debounced 3-column `ArticleCard` results grid, the shared `EmptyState` for a no-results query, Framer Motion fade+slide enter/exit respecting `prefers-reduced-motion` via `useReducedMotion()`, closes on the × button, an outside/backdrop click, or `Escape`. Added `src/hooks/use-search-overlay.tsx` (a small React Context provider, mirroring the existing `useAuthModal` pattern) and wired it into `Providers.tsx` plus the Navbar's previously-inert search icon button. Built the deep-linkable `src/app/(marketing)/search/page.tsx` (same `searchArticles` call, plain grid, no overlay chrome, for shareable `/search?q=...` URLs). Popular pills are fetched once server-side in the root `layout.tsx` (via `getPopularCategoryPills`) and passed down through `Providers`, so the overlay's pill row needs no additional client-side fetch of its own. **Real bug found and fixed via live testing**: `ArticleCard`'s `timeAgo()` call crashed with `date.getTime is not a function` whenever `ArticleCardData` came from the search overlay's `fetch().json()` response — `publishedAt` survives a JSON round-trip as a plain ISO string, not a real `Date` object (unlike every other page, which gets a real `Date` straight from a Server Component's Drizzle query). Fixed by widening `timeAgo()`'s param type to `Date | string | null` and normalizing with `new Date(date)` when it isn't already a `Date` instance, and updated `ArticleCardData.publishedAt`'s type to `Date | string | null` to make this contract explicit rather than silently relying on a lie in the original `Date`-only type. **Live end-to-end testing performed** (headless Chrome via Puppeteer against the real dev server and Neon DB, 8 real published test articles across 5+ categories, one tagged "History" to exercise the tag-search path): clicking the navbar search icon opens the overlay with the input correctly autofocused; the 5 real top-category pills render (not placeholder English words); typing "javascript" returns live results within ~1s including the exact matching article, confirmed via both DOM-text assertion and a full non-fullPage screenshot showing a fully opaque white overlay with the correct single result card; typing a nonsense query correctly shows the "No results for ..." EmptyState with a "Browse all content" CTA; clicking a popular pill fills the query and immediately triggers a search; `Escape` closes the overlay; the deep-linkable `/search?q=javascript` page independently renders the same result server-side. (One early screenshot appeared to show homepage content bleeding through below the overlay — diagnosed as a `fullPage: true` Puppeteer screenshot artifact, since a `position: fixed` element only ever covers the real viewport and `fullPage` stretches the capture to the underlying document's full scroll height; a normal non-fullPage screenshot confirmed the overlay is genuinely fully opaque with no real rendering bug.) All test articles/users cleaned from the DB afterward, all scratch Puppeteer/seed scripts and screenshots deleted, confirmed via `git status` that only real Step 6 source files remain. `npx tsc --noEmit` and `npm run build` both pass with zero errors; final build's route listing confirms `/api/search` and `/search` registered correctly. | **Process note for future steps**: any `ArticleCardData` (or similarly-shaped data) that might pass through a `fetch()`/JSON API boundary — as opposed to arriving straight from a Server Component's DB query — must treat its date fields as `Date \| string`, never assume they stay `Date` instances; `timeAgo()` now normalizes this centrally so any future client-fetched list reusing it is already safe. When testing a `position: fixed` full-screen overlay with Puppeteer, prefer a normal (viewport-sized) screenshot over `fullPage: true` — the latter resizes to the document's full scroll height and will visually "reveal" whatever sits behind a fixed overlay, which looks exactly like a transparency bug but isn't one. |
| 7 — AuthorPro Upgrade + Stripe Billing | ✅ Done (4-phase loop complete) | Built `src/lib/stripe.ts` (Stripe client from `STRIPE_SECRET_KEY`). **Product/Price sync approach chosen: inline `price_data` at Checkout-session-creation time, no pre-created Stripe Product/Price objects.** Since `platformConfig.authorProMonthlyCents`/`authorProYearlyCents` are admin-editable at any time, generating the Stripe line item inline from the current config value on every checkout call means pricing is always exactly correct with zero sync/drift risk — the alternative (pre-created Prices, updated whenever an admin changes config) would need its own reconciliation logic for no real benefit at this stage. Built `src/app/api/checkout/subscription/route.ts` (POST, `requireAuth()`-gated, validates `{interval: "monthly"|"yearly"}`, looks up or creates a Stripe Customer — reusing `stripeCustomerId` from the user's most recent prior `subscriptions` row if one exists — creates a subscription-mode Checkout Session with `price_data`/`recurring`, `success_url=/dashboard/author/billing?success=true`, `cancel_url=/dashboard/author/billing?canceled=true`). Built `src/app/api/webhooks/stripe/route.ts`: reads the raw request body (Next.js App Router route handlers never auto-parse the body, so `request.text()` already yields the exact bytes Stripe signed — no special config needed, unlike the Pages Router's `bodyParser: false`), verifies the signature via `stripe.webhooks.constructEvent` against `STRIPE_WEBHOOK_SECRET`, and handles `checkout.session.completed` (author_pro subscription mode only — inserts the `subscriptions` row with `status`/`billingInterval`/`currentPeriodStart`/`currentPeriodEnd` read straight from the retrieved Stripe subscription object, `stripeSubscriptionId`, `stripeCustomerId`), `customer.subscription.updated`, and `customer.subscription.deleted` (both keep an existing row's `status`/period dates in sync — cancelled/active/past_due mapped from Stripe's subscription status). Built `src/lib/queries/subscriptions.ts` (`getAuthorProSubscription`, `getPlatformConfig`) and the billing page `(dashboard)/dashboard/author/billing/page.tsx`: shows current status (Not subscribed / AuthorPro Active with renewal date / Cancelled / Past Due), and when not active, `src/components/billing/GoProSection.tsx` — a monthly/yearly toggle with pricing pulled live from `platformConfig` (never hardcoded), a "Go Pro" button that POSTs to the checkout route and redirects the browser to Stripe's hosted Checkout page. **Live end-to-end testing performed with a real Stripe TEST-mode account** (not mocked): started `stripe listen --forward-to localhost:3000/api/webhooks/stripe --api-key <real sk_test_ key>` (the CLI on this machine is logged into an unrelated Stripe account by default — user confirmed passing `--api-key` explicitly rather than switching the CLI's global login), got a real webhook signing secret, set it in `.env.local`, restarted the dev server. A verified test Author's full flow was driven via headless Chrome (real login+OTP, not mocked): billing page correctly showed "Not subscribed" and the Go Pro section; clicking "Go Pro" (monthly) redirected to a genuine `checkout.stripe.com` URL; filled Stripe's official test card `4242 4242 4242 4242` and completed checkout for real; redirected back to `/dashboard/author/billing?success=true`; after the webhook round-trip, a page reload showed "AuthorPro Active" with the correct renewal date. Confirmed via direct DB query that the `subscriptions` row has `type=author_pro`, `status=active`, a real `stripeSubscriptionId`, and `currentPeriodEnd` in the future — i.e. `requireAuthorPro()` from Step 2.6 now genuinely returns true for this user, unblocking Steps 8/9. Also tested the cancellation path: cancelled the same subscription directly via the Stripe CLI (`stripe subscriptions cancel`), confirmed the `customer.subscription.deleted` webhook fired, was signature-verified, returned `200`, and correctly flipped the DB row's `status` to `cancelled`. Every webhook event in the CLI's log (`checkout.session.completed`, `customer.subscription.created`, `invoice.payment_succeeded`, `customer.subscription.deleted`, and several Stripe-internal events our handler correctly no-ops on) returned `200`. All test data cleaned up afterward (test author user + its `subscriptions` row deleted from the DB; Stripe's own test-mode customer/subscription objects were left in place, as is normal practice for Stripe test accounts and costs nothing). All scratch Puppeteer/seed/cleanup scripts deleted, confirmed via `git status` that only real Step 7 source files remain and `.env.local` stayed untracked (confirmed via `git check-ignore`). `npx tsc --noEmit` and `npm run build` both pass with zero errors; final build's route listing confirms `/api/checkout/subscription`, `/api/webhooks/stripe`, and `/dashboard/author/billing` all registered. | **Process note for future steps (Step 8's pay-per-article checkout extends this same webhook handler)**: the webhook route must keep branching on `event.type` and, within `checkout.session.completed`, on `session.mode`/`session.metadata` — Step 8's one-time `payment` mode events must not be handled by the `subscription`-mode branch built here. Any new local Stripe testing session needs a fresh `stripe listen ... --api-key <real key>` run and a matching `.env.local` update + dev server restart, since the signing secret is per-listen-session, not static. |
| 8 — Premium/Paywall + Pay-Per-Article Checkout | ✅ Done (4-phase loop complete) | **Rounding-remainder rule:** every split is computed with `Math.floor(gross * pct / 100)` per share, then whatever's left over after all integer-floor shares are subtracted from the gross is added entirely to the platform's share — guarantees the parts always sum exactly to the gross in integer cents with no floating-point drift, at the cost of the platform (not the author) absorbing the rounding remainder; documented in `src/lib/revenue-split.ts`'s `splitCents()`. Extended `src/lib/validators/article.ts`'s `articleSchema` with `isPremium`/`priceCents`, and `src/lib/actions/article.ts` with `resolvePremiumFields()` (used by both `createArticleAction`/`updateArticleAction`): re-checks `requireAuthorPro()` fresh on every save regardless of client state, validates the price against `platformConfig.payPerArticleMinCents/MaxCents`, and silently downgrades to a free article on any violation — no error thrown, since the article content itself is still valid, just not monetizable as requested. Added `getPremiumEligibilityAction()` (purely informational, used only to decide whether the ArticleForm shows the Premium toggle at all — never trusted for enforcement). Updated `ArticleForm.tsx`: a Premium checkbox + price input, shown only when `eligibility.isAuthorPro` and there are zero co-authors (joint articles are always free, no UI path around it). Built `src/lib/permissions.ts`'s `hasArticleAccess(userId, article)` — a plain boolean (not a throwing `require*()`, since the article page needs to render a paywall rather than redirect) checking, in order: article not premium → always true; a `purchases` row for this user+article; an active `platform`-type subscription; an active `publication`-type subscription matching the article's `publicationId`. Built the paywall UI on `(marketing)/article/[slug]/page.tsx`: body rendered in a `max-h-[420px] overflow-hidden` container with a bottom gradient-fade-to-white mask, and `PaywallCard.tsx` centered on top (lock icon, "This is a Premium article" heading, a "Buy this article — $X.XX" button; the user confirmed **Buy-only for now** — "Subscribe to Publication"/"Subscribe to Platform" options are omitted entirely rather than shown as broken/disabled buttons, since Publications (Step 9) and Reader subscriptions (Step 10) don't exist yet). Built `src/app/api/checkout/article/route.ts` (POST, `requireAuth()`-gated, validates the article is actually premium/published/priced, rejects with 409 if this user already has a `purchases` row for it, creates a one-time `payment`-mode Checkout Session with inline `price_data`, `articleId`/`userId`/`purchaseType` in metadata). Extended `src/app/api/webhooks/stripe/route.ts`'s `checkout.session.completed` handler with a `payment`-mode branch: idempotent on `stripePaymentIntentId` (a re-delivered webhook event won't double-insert), inserts the `purchases` row, then calls the new `src/lib/revenue-split.ts`'s `calculateAndRecordSplit(articleId, grossAmountCents, "purchase", purchaseId)` — reads `platformConfig` fresh, branches on whether the article has a `publicationId` (standalone 80/20 vs in-publication 60/20/20 per `00_ScopeDocument.md` §5.1), and inserts the `ledger` row with `payoutStatus="pending"` and a `TODO` comment pointing at §5.3/§11 for the real Stripe Connect transfer (Phase 2, correctly not attempted here). Implemented the optimistic-unlock requirement from the guide's Section 5: on the success redirect (`?purchased=true&session_id=...`), the article page independently verifies the Checkout Session directly with Stripe (paid, metadata matches this article+user) and grants access for that render even if the webhook hasn't landed yet — the webhook remains the sole source of truth for every subsequent visit. **Live end-to-end testing performed with real Stripe TEST-mode traffic** (`stripe listen` running throughout, a genuine AuthorPro-subscribed test Author, a separate test Reader account, real login+OTP where applicable): an unauthenticated visitor and a Reader with no purchase both see the truncated body + paywall card with the correct price; clicking "Buy this article" redirects to a genuine `checkout.stripe.com` session; completing checkout with Stripe's `4242 4242 4242 4242` test card redirects back with `purchased=true`+`session_id`, and the article is unlocked immediately (optimistic access, confirmed before assuming the webhook had necessarily landed); after the webhook round-trip, a fresh page load (new checkout-session-less URL) still shows the article unlocked, confirmed via direct DB query that this is backed by a real `purchases` row (not just the optimistic path) with `amount_cents=500` and a real `stripePaymentIntentId`; the matching `ledger` row was verified to have `gross_amount_cents=500`, `author_cents=400`, `platform_cents=100`, `publication_owner_cents=null` — exactly 80/20 for a standalone article, summing exactly to the gross with zero drift; a second purchase attempt via the API for the same user+article was correctly rejected with 409; a completely unauthenticated, cookie-isolated visitor (verified via an isolated Puppeteer browser context, not just a second tab/page which shares cookies) still sees the paywall for the now-purchased article, confirming access is genuinely per-user. Separately verified the Premium-toggle server enforcement end-to-end via the live UI: the AuthorPro author sees the toggle, checking it and entering a valid price (`$4.99`) and publishing solo (no co-authors — the co-author search correctly returns nothing for a `reader`-role account, confirming Step 4's role-scoped search still holds) persists `isPremium=true, priceCents=499` to the DB exactly as entered. All test authors/readers/articles/purchases/ledger rows/subscriptions removed from the DB afterward; all scratch Puppeteer/seed/cleanup scripts deleted; confirmed via `git status` that only real Step 8 source files remain and `.env.local` stayed untracked. `npx tsc --noEmit` and `npm run build` both pass with zero errors; final build's route listing confirms `/api/checkout/article` registered alongside the existing checkout/webhook routes. | **Real debugging note for future sessions**: mid-testing, an unrelated project's dev server on this machine grabbed port 3000 (which `AUTH_URL` points to) between test runs, causing Next.js to silently fall back to port 3002/3010 — several apparent "Premium toggle doesn't persist" test failures were actually Puppeteer driving a completely different website on port 3000, not a real bug (confirmed by inspecting the page's actual `aria-label`s, which didn't match Contributor's Navbar at all). User chose to manage this port conflict manually going forward rather than permanently reassign Contributor's port — see the note in Current Position above. **Also confirmed as test-script artifacts, not bugs, during this step's investigation**: Puppeteer's `browser.newPage()` shares cookies with all other pages in the same browser instance by default — testing "does an unauthenticated visitor see X" requires `browser.createBrowserContext()` for genuine cookie isolation, not just a second page/tab. |
| 9 — Publications (Create/Invite/Accept/Decline) | ✅ Done (4-phase loop complete) | Built `src/lib/actions/publication.ts`: `createPublicationAction` (re-checks `requireAuthorPro()` fresh, generates a unique slug), `getMyPublicationsAction` (owned + accepted-invite Publications), `getPublicationForManagementAction`/`getPublicationArticlesAction` (Owner-only, throws `ForbiddenError` otherwise), `searchContributorCandidatesAction` (mirrors Step 4's author/admin-only, active-only, self-excluded pattern — no `username` column exists in the schema, so per the guide's own username/email wording being aspirational, search is by name/email exactly like the existing co-author search), `sendInviteAction` (Owner-only, rejects self-invite and a second pending invite to the same person, writes the `invites` row + a `publication_invite` notification, and best-effort sends `publication-invite.tsx` via Resend — a send failure is logged but never rolls back the invite/notification, same pattern as Step 2.3's `signupAction`), `getMyPendingInvitesAction`/`respondToInviteAction` (accept/decline, writes `invite_accepted`/`invite_declined` notifications for the Owner, rejects responding to an already-responded invite), `getSelectablePublicationsAction` (owned + accepted-invite Publications, used only by the article editor's Publication dropdown — deliberately a separate list/mechanism from Step 4's co-author search, per the guide's explicit point 6). Built `src/app/api/users/search/route.ts` as the guide's requested reusable endpoint (same filtering rules as the action version). Built pages: `(dashboard)/dashboard/author/publications/page.tsx` (list, owner vs. contributor role shown), `publications/new/page.tsx` (checks `requireAuthorPro()` itself and renders a `UpgradePrompt` component instead of redirecting for a free Author — never a bare 403, per the guide's explicit instruction), `publications/[id]/page.tsx` (Owner-only management view, Contributors/Articles tabs via a small page-local `PublicationManagementTabs` client component — not a shared abstraction, since this is the first and only tabbed UI in the app so far), `(dashboard)/dashboard/author/invites/page.tsx` (added to `DashboardSidebar.tsx`'s Author nav, which didn't have an Invites link yet), and the public `(marketing)/publication/[slug]/page.tsx` (cover/name/description/owner byline + the Publication's published articles via the existing shared `ArticleCard`). Extended `articleSchema`/`ArticleForm.tsx`/`article.ts`'s actions with a `publicationId` field and a new `resolvePublicationId()` enforcement helper: a submitted `publicationId` is silently dropped (article saves as standalone) unless the caller genuinely owns that Publication or holds an accepted invite to it — never trusts the client value, checked fresh on every save exactly like the existing Premium enforcement. The editor's Publication dropdown is populated only via `getSelectablePublicationsAction`, confirmed structurally separate from the co-author search (an article can independently have both a Publication assignment and co-authors — they compose rather than being the same mechanism). Confirmed via the live test below that a free-tier (non-AuthorPro) contributor publishing into an accepted Publication still correctly cannot mark that article Premium — Publication membership never grants monetization ability on its own, only the contributing Author's own AuthorPro status does, per the guide's "at the contributing Author's discretion" framing. **Live end-to-end testing performed with real Stripe TEST-mode traffic and real login+OTP** (an AuthorPro-subscribed test Owner, a free-tier test Contributor, and a test Reader, all via the real dev server and Neon DB): the Owner sees the real "New Publication" form (not an upgrade prompt); creating a Publication redirects to its management page; searching for the Contributor by partial email finds them; sending the invite creates a `pending` `invites` row and a `publication_invite` notification for the Contributor (both DB-verified); the Contributor sees the pending invite on `/dashboard/author/invites` and accepting it flips the row to `status=accepted` with `respondedAt` set and creates an `invite_accepted` notification for the Owner (DB-verified); the free-tier Contributor publishing an article into the now-accepted Publication correctly does NOT see the Premium toggle and the article correctly saves as free with the right `publicationId` (DB-verified); separately, the AuthorPro Owner published their own Premium ($10.00) article into their own Publication, which a Reader then purchased end-to-end with Stripe's `4242 4242 4242 4242` test card — the resulting `ledger` row was confirmed via direct DB query to have `gross_amount_cents=1000`, `author_cents=600`, `publication_owner_cents=200`, `platform_cents=200` (exactly 60/20/20, summing exactly to the gross); the public `/publication/[slug]` page was confirmed to render the Publication's name and list its published article. All test users/publications/invites/notifications/articles/purchases/ledger rows removed from the DB afterward; all scratch Puppeteer/seed scripts deleted; `.env.local`'s `AUTH_URL` (temporarily changed to port 3010 to route around the port-3000 conflict, see Current Position) was reverted back to port 3000 afterward; confirmed via `git status` that only real Step 9 source files remain. `npx tsc --noEmit` and `npm run build` both pass with zero errors; final build's route listing confirms all new Publications/Invites routes and `/api/users/search` registered correctly. | **Process notes for future sessions**: (1) several apparent UI-text-match test failures during this step's testing turned out to be pure Puppeteer timing races against Neon's HTTP-mode query latency and cold Turbopack route compiles — the DB state itself was correct in every case investigated, so when a live-tested flow's DB assertions all pass, don't over-trust a same-run innerText check that disagrees; re-verify against the DB directly before concluding a bug exists. (2) This step's first `npm run build` attempt appeared to hang for 9+ minutes; investigation showed the actual build process (a different PID than initially suspected) was still genuinely making progress (compile finished in 3.8min, which is slow but real) — killing what looked like a stuck process actually killed the live build mid-typecheck. Confirm a process's CPU-time is truly flat across two checks a few minutes apart before concluding it's hung, rather than assuming from wall-clock time alone. |
| 10 — Reader Subscriptions + Pooled Revenue Ledger | ✅ Done (4-phase loop complete) | Extended `src/app/api/checkout/subscription/route.ts` (from Step 7) to also handle `type="publication"` (validates `publicationId`, prices from `publicationSubMonthlyCents`/`YearlyCents`) and `type="platform"` (prices from `platformSubMonthlyCents`/`YearlyCents`) alongside the existing `author_pro` path, all sharing the same inline-`price_data` approach and Stripe-customer-reuse logic. Extended `src/app/api/webhooks/stripe/route.ts`'s `upsertSubscriptionFromStripe` to accept a generic `type`/`publicationId` instead of being hardcoded to `author_pro`, and added the supersede step: before inserting a new `platform`-type row, it looks up any existing active `publication`-type subscription for that user, cancels it via `stripe.subscriptions.cancel`, and marks it `superseded` locally — implementing docs/00_ScopeDocument.md Section 6 / Flow J exactly (Platform always supersedes an active Publication sub; the reverse is allowed, never blocked, per spec). Built `src/app/(dashboard)/dashboard/reader/subscriptions/page.tsx`: lists the Reader's Publication/Platform subscriptions with status/renewal date and a Cancel action (`cancelSubscriptionAction`, cancels in Stripe then marks `cancelled` locally), and — only when no active Platform subscription exists — a "Subscribe to the Platform" section whose buttons show a `window.confirm` warning dialog (via a new reusable `SubscribeButton`/`confirmMessage` prop) if the Reader currently holds an active Publication subscription, clearly explaining it will be superseded, per the guide's explicit "must be ALLOWED, not blocked — just clearly communicated" instruction. The same warning was also wired into the paywall card's own "Subscribe to the Platform" button (`PaywallCard.tsx`, via a new `getActivePublicationSubscriptionName` lookup fetched only when the paywall actually renders) — the guide's literal text only mentions the dedicated subscriptions page, but a Reader could just as easily trigger the supersede from the paywall, so the same warning applies there too. Extended `src/lib/permissions.ts`: replaced `hasArticleAccess`'s boolean-only return with a new `getArticleAccessSource()` that also reports *which* access path granted it (`free`/`purchase`/`platform_subscription`/`publication_subscription`) plus the granting subscription's `currentPeriodStart` — needed to correctly bucket a qualifying read into its billing period; `hasArticleAccess` now just wraps this for any caller that only needs the boolean. Extended `src/lib/revenue-split.ts` with `logQualifyingRead()` (inserts a `readEvents` row, `onConflictDoNothing` against the exact `(userId, articleId, billingPeriodStart)` unique constraint from Step 1, so re-reads in the same period never double-count) and `distributePooledSubscriptionRevenue(subscription, grossAmountCents)`: queries `readEvents` for that user within `[subscription.currentPeriodStart, subscription.currentPeriodEnd)`, counts distinct premium `articleId`s read, divides the gross evenly (integer division), and runs each article's share through the existing `calculateAndRecordSplit` (so in-publication articles still get 60/20/20 and standalone articles 80/20 even inside a pooled distribution) — with `sourceType` set to `platform_subscription` or `publication_subscription` accordingly. **Rounding-remainder rule (as required to be documented):** the leftover remainder cent(s) from the even integer division go to the FIRST article read in the period (ordered by each article's earliest `readEvents.createdAt`), not to the platform — a deliberate difference from `calculateAndRecordSplit`'s own per-article percentage-split remainder (which does go to the platform), since these are two different remainder problems: one is "dividing a whole dollar amount across N articles" (arbitrary tie-break needed, chose first-read), the other is "splitting one article's share into percentage buckets" (platform absorbs the drift, per Step 8's existing rule). **Publication-scoped pooling choice (as required to be documented):** implemented the simpler of the guide's two offered options — a Publication subscription's revenue is pooled only across that one Publication's distinct articles read in the period, chosen because a Publication subscriber structurally cannot have qualifying reads outside that Publication in the first place, making the two documented options equivalent for this subscription type while the simpler one needs no extra scoping code (documented inline in `revenue-split.ts` per the guide's explicit instruction to reference this decision in comments). Per the guide's explicit instruction, a billing period with zero qualifying reads still writes a ledger row (`articleId=null`, 100% `platformCents`, a `splitPercentagesUsed` note explaining why) rather than silently doing nothing. Wired `logQualifyingRead` into `(marketing)/article/[slug]/page.tsx`: fires (fire-and-forget, a logging failure never breaks the page render) only when `getArticleAccessSource` reports `platform_subscription` or `publication_subscription` — never for a `purchase`, which is already fully attributed at purchase time and must not also participate in pooled distribution. Added the `invoice.payment_succeeded` case to the webhook handler, calling `distributePooledSubscriptionRevenue` with the subscription row's **current** (pre-renewal-update) period bounds — deliberately read directly from the DB row rather than parsed off the Stripe `Invoice` object, since `Invoice`'s period-bearing fields differ across Stripe API versions (confirmed the hard way — see bugs below) and this approach doesn't depend on webhook delivery ordering relative to `customer.subscription.updated`. **Two real bugs found and fixed during a careful code-review pass** (see Known Issues — this step was NOT live-tested this session): (1) **API-version type error, would have failed to build**: `invoice.subscription` does not exist on this pinned Stripe SDK version's `Invoice` type — the subscription reference moved to `invoice.parent.subscription_details.subscription`; caught immediately by `tsc`, not a logic bug, but confirms the guide's Step 10 text (written against an unspecified/older Stripe API shape) needs a small adaptation for this project's actual installed Stripe version. (2) **Real logic bug, would have silently written wrong data**: the very first `invoice.payment_succeeded` event for a brand-new subscription (fired at initial checkout) was originally unconditionally running `distributePooledSubscriptionRevenue` — but a brand-new subscription's `currentPeriodStart`/`End` at that moment describe the period that is just *starting*, not one that "just ended," and zero reads could possibly exist yet, so this would have written a premature, semantically-wrong "100% to platform, zero reads" ledger row one billing cycle too early on every single new Platform/Publication subscription, and then never correctly attributed the *actual* first completed period. Fixed by checking Stripe's `invoice.billing_reason === "subscription_cycle"` (renewal) before distributing, skipping `"subscription_create"` (the initial invoice) entirely. (3) **Real logic bug, would have silently lost state**: cancelling the superseded Publication subscription via `stripe.subscriptions.cancel()` (to implement the supersede rule) itself fires a `customer.subscription.deleted` webhook back at this same handler, which was unconditionally setting `status="cancelled"` — overwriting the `"superseded"` status just set moments earlier in the very same request flow, permanently losing the distinction between "the Reader cancelled this themselves" and "this was replaced by a Platform subscription." Fixed by skipping the `customer.subscription.updated`/`.deleted` status-overwrite whenever the existing row's status is already `"superseded"`. Also removed one piece of dead code found during review (`hasActivePublicationSubscriptionAction`, written but never called from anywhere — the subscriptions page already derives the same fact from its own already-fetched list). `npx tsc --noEmit` and `npm run build` both pass with zero errors after all three fixes; final build's route listing confirms `/dashboard/reader/subscriptions` registered correctly. **Live end-to-end testing performed in a follow-up session with real Stripe TEST-mode traffic** (dev server + `stripe listen` on port 3010 to route around the recurring port-3000 conflict, `AUTH_URL` temporarily repointed and restored afterward): seeded a test Reader, an AuthorPro-subscribed Publication Owner, and two Premium ($5.00 equivalent list price, actually priced via `publicationSubMonthlyCents`) in-Publication articles. Confirmed the paywall correctly shows before any subscription exists. As the Reader, subscribed to the Publication with Stripe's `4242 4242 4242 4242` test card — DB-verified the resulting `subscriptions` row is `type="publication"`, `status="active"`, correctly linked via `publicationId`; confirmed both of that Publication's Premium articles render fully unlocked afterward, and the Subscriptions page lists it correctly. Visited both articles as the Reader — DB-verified exactly 2 `readEvents` rows were logged (one per distinct article), then re-visited the first article again and confirmed NO third row was created (the `(userId, articleId, billingPeriodStart)` dedup constraint is genuinely working, not just theoretically correct). Then, while the Publication subscription was still active, subscribed to the Platform as the same Reader — the `window.confirm` supersede warning fired with the exact expected copy naming the Publication by name; after completing that Stripe checkout too, DB-verified the Publication subscription's status flipped to `"superseded"` (not `"cancelled"`) while the new Platform subscription is `"active"`, AND independently confirmed via a live Stripe API call (`stripe.subscriptions.retrieve`) that the Publication subscription's Stripe-side status is genuinely `"canceled"` — proving bug-3's fix holds under a real webhook round-trip, not just in code review. For the pooled-distribution logic itself: since Stripe's `stripe trigger invoice.payment_succeeded` synthesizes its own unrelated subscription/invoice objects (confirmed by inspecting `stripe trigger --help`'s documented behavior) and so cannot be made to reference a real locally-created test subscription, `distributePooledSubscriptionRevenue` was called directly with the real Platform subscription row and a real gross amount ($14.99 = 1499 cents, matching the seeded `platformSubMonthlyCents`) — exactly the call the webhook handler itself makes, exercising the actual distribution logic rather than a synthetic Stripe event. Result DB-verified exactly correct: two `ledger` rows, `750 + 749 = 1499` cents (exact even split with the 1-cent remainder correctly on the first-read article, per the documented rounding rule), each further split 60/20/20 (both test articles were in-Publication) summing exactly to their own gross with zero drift. Separately re-ran the same call with a fabricated future period containing zero `readEvents` and confirmed the zero-qualifying-reads branch fires exactly as documented: one `ledger` row, `articleId=null`, `authorCents=0`, `publicationOwnerCents=null`, `platformCents=1499` (100%), with the `splitPercentagesUsed` explanatory note. All test users/publications/articles/subscriptions/readEvents/ledger rows removed from the DB afterward (in FK-safe order — subscriptions/ledger before publications, articles before publications); all scratch scripts deleted; `.env.local`'s `AUTH_URL` reverted to port 3000; confirmed via `git status` that the working tree is clean. | Step 10 is now held to the same live-tested bar as every other completed step in this build — see the Known Issues entry below for the resolution note (kept for history rather than deleted, since it documents exactly which bugs manual review alone had already caught before this live-test pass confirmed the fixes hold in practice). |
| 11 — Admin Dashboard | ✅ Done (4-phase loop complete) | Built under `src/app/(dashboard)/dashboard/admin/` (matches the established `dashboard/author`/`dashboard/reader` route convention, not the guide's literal `admin/` path text): overview stat tiles, paginated/searchable Users table (Verify email, Suspend/Unsuspend), Moderation queue + detail view (Dismiss/Unpublish/Suspend Author, all audit-logged with actionedByUserId+actionedAt), Category CRUD (create/rename/deprecate, soft-delete only), Fee Settings form (all prices + both revenue-split groups with live 100%-balance validation blocking submit). All server actions in `src/lib/actions/admin.ts`, gated by `requireRole("admin")` (OTP-verified). Found and fixed two pre-existing bugs while building: suspended authors' articles were never actually hidden from public queries (fixed in `src/lib/queries/articles.ts`), and the app had no `not-found.tsx` so every `notFound()` call site rendered blank (fixed via new `src/app/not-found.tsx`). Live-verified the fee-config change end-to-end: changed standalone split to 70/30, ran a real Stripe purchase, confirmed the new ledger row used 70/30 exactly, then reverted to 80/20. | Route path deviates from the guide's literal `src/app/(dashboard)/admin/` text — used `dashboard/admin/` instead to match the app's actual existing convention for author/reader dashboards. |
| 12 — Frontend Article Editor (Medium-Style) | ✅ Done (4-phase loop complete) | User confirmed the guide's proposed UX description verbatim (minimal chrome, ~720px serif reading-width column, selection-triggered floating toolbar with Bold/Italic/H2/H3/Link/Blockquote, inline click-to-embed images) before any code was written — no reference video was available in the project. Installed `@tiptap/react` v3 + `starter-kit`/`extension-image`/`extension-placeholder` (link comes bundled in v3's starter-kit, not a separate package). Built `src/components/editor/EditorCanvas.tsx` (title input + Tiptap content area), `FloatingToolbar.tsx` (Tiptap v3's `BubbleMenu` from the `@tiptap/react/menus` subpath — NOT the old `@tiptap/extension-bubble-menu` package directly — with Framer Motion fade+scale), `PublishSettingsDrawer.tsx` (category/tags/cover/Premium/co-authors moved into a Base UI slide-in drawer so the canvas stays the visual focus). Body persists as the same `{html: string}` JSON shape Step 4 already used, so the public article page needed zero changes. | Found and fixed two knock-on bugs this change would have caused: `buildExcerpt()` in `src/lib/actions/article.ts` and the body validator in `src/lib/validators/article.ts` both still treated `body` as plain text — now both strip HTML tags first (excerpt) or check for real stripped-tag content before validating as non-empty (validator), since Tiptap's "empty" state serializes as `<p></p>` which a naive `.min(1)` string check passes. Also found and fixed a real mobile UX bug via Playwright device-emulation testing: the sticky top action bar (Publish settings / Save Draft / Publish) wrapped to two cramped rows on a 375-414px viewport — fixed with icon-only buttons and a hidden step label below `sm:`. Confirmed (and intentionally left as-is) that the floating toolbar can visually sit over the title only in the artificial edge case of selecting the very first character of the very first line the instant after typing it — this self-resolves for any realistic selection further into the content, matches real Medium's own behavior, and is not worth constraining further. Verified backward compatibility explicitly: an old Step-4-style plain-text article (pre-existing `{html: "plain text"}` body, no real HTML tags) loads correctly into the new editor and still renders correctly on the public page. |
| 13 — Notifications & Emails | ✅ Done (4-phase loop complete) | Added `purchase-receipt.tsx`, `moderation-notice.tsx`, `invite-response-notice.tsx` Resend templates (matching the existing `publication-invite.tsx`'s on-brand inline-style pattern). Wired into: the Stripe webhook's `checkout.session.completed` handler (receipt email on article purchase AND any new subscription; `author_pro_activated` notification row specifically for AuthorPro), `unpublishArticleAction`/`suspendAuthorForReportAction` in `src/lib/actions/admin.ts` (moderation-notice email + notification row to the affected author, including the report's reason), and `respondToInviteAction` (invite-response-notice email to the Publication owner — the notification row already existed from Step 9). Built the in-app bell: `src/components/dashboard/NotificationBell.tsx` in `DashboardTopbar`, backed by new `src/lib/actions/notifications.ts` (get list, get unread count, mark-one-read, mark-all-read). | Verified all 3 newly-wired flows end-to-end with Playwright (invite-accept, article purchase, moderation-unpublish) — confirmed correct bell content/read-state and correct DB rows in each case, and rendered all 4 email templates to local HTML to visually confirm on-brand styling since Resend send itself still fails on the Step 2.3 placeholder-API-key waiver (not a real bug — `render()` succeeds, only the actual Resend API call 401s). |
| 14 — Full Responsive Pass | ⬜ Not started | | *(Log which mobile dashboard-sidebar pattern was chosen — drawer/bottom-nav/hamburger.)* |
| 15 — Full Animation/Motion Polish Pass | ⬜ Not started | | |
| 16 — SEO / Performance / Accessibility Pass | ⬜ Not started | | *(Log before/after Lighthouse scores.)* |
| 17.1 Deploy to Vercel | ⬜ Not started | | |
| 17.2 Production Stripe webhook | ⬜ Not started | | |
| 17.3 Custom domain + DNS | ⬜ Not started | | |
| 17.4 Stripe live mode cutover | ⬜ Not started | | |

### Verification & Test-Case Prompts (run after Step 17, or opportunistically after Step 13)

| Prompt | Status | Summary of findings | Notes |
|---|---|---|---|
| Test-Case Prompt 1 — Cross-role edge-case & bug hardening | ⬜ Not run | | |
| Test-Case Prompt 2 — Responsive & cross-device QA | ⬜ Not run | | |
| Test-Case Prompt 3 — Performance & accessibility audit | ⬜ Not run | | |
| Scope Completeness Audit Prompt | ⬜ Not run | | |
| Whole App Flow Prompt | ⬜ Not run | | |

---

## Known Issues / Open Items

> Anything flagged `⚠️` above, or any bug found and deferred rather than fixed immediately, gets logged here with enough detail for a future agent to pick up without re-investigating from scratch.

- **Step 2.3 — real email delivery never tested (by user's explicit choice).** `RESEND_API_KEY`/`RESEND_FROM_EMAIL` in `.env.local` are still placeholders. User instructed (2026-09-05) to skip real-inbox testing and mark the step done anyway; everything else is built and tested (see Step 2.3's row above). If real Resend credentials are added later, a quick sanity check (one live signup, confirm the email arrives and the link verifies) is worth doing opportunistically, but is not blocking anything.

- **Step 10 — Reader Subscriptions / pooled ledger: RESOLVED (2026-09-09), now live-tested and fully verified.** Originally built and only code-reviewed on 2026-09-07 due to severe unrelated machine resource contention that session (a `RuntimeSync` process with 65,000+ seconds of accumulated CPU time, several `Devin` agent processes, `Cursor`, and `Exodus` all competing for CPU — a single homepage request took 2.9 minutes to resolve). A follow-up session on 2026-09-09, with the machine clear, ran the full live-test plan end-to-end with real Stripe TEST-mode traffic: Publication subscribe → access grant → qualifying-read logging + dedup → Platform subscribe with supersede (DB status `superseded` AND independently-confirmed-canceled in real Stripe, not just locally) → pooled distribution math (called `distributePooledSubscriptionRevenue` directly with the real subscription row and gross amount, since Stripe's `stripe trigger invoice.payment_succeeded` synthesizes an unrelated subscription/invoice and cannot be pointed at a real local test subscription) → exact ledger verification (both the multi-article-with-remainder case and the zero-qualifying-reads case). See Step 10's row above for full detail. All three bugs caught during the original code-review pass (API-version `invoice.subscription` field, premature-first-invoice distribution, superseded-status-overwrite) were confirmed fixed and holding under real webhook traffic, not just in theory. This entry is kept for history rather than deleted, per the file's own convention of not silently dropping a step's story.

---

## How to Update This File (for the agent)

At the end of any step/sub-step:
1. Change its row's Status column.
2. Fill in "What was actually done / decided" — 1-2 plain sentences, e.g. "Created all 15 tables + seeded platformConfig with 80/20/60/20/20 defaults, migrations ran clean against Neon."
3. If you deviated from the guide's exact instructions for a documented reason, note it in "Notes / deviations" — don't silently diverge.
4. If a sub-step involved a "your choice, document which" decision or a required user confirmation, add a row to the **Key Decisions Log**.
5. Update **Current Position** at the top: flip "Next up" to the next step, update "Last updated by" with today's date and a short session identifier.
6. If you found and fixed a bug outside the current step's direct scope, or deferred one, log it in **Known Issues / Open Items**.
